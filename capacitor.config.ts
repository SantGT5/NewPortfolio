import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'newportfolio',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
